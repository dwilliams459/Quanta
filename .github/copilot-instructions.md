# Copilot Instructions

## Project Guidelines
- User prefers simpler, minimal implementations over complex fallback logic; for VS Code launch failures, show a dialog instructing to ensure 'code' is on PATH.
- For markdown generation in ViewLog, provide a single Generate Markdown action with an 'include events' checkbox, rather than separate buttons.